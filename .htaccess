RewriteEngine On

# If the request is for a valid file, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, route everything to bot.php
RewriteRule ^(.*)$ bot.php [QSA,L]
